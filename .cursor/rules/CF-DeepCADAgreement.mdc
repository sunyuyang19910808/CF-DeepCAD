---
alwaysApply: true
---

# CF-DeepCAD 架构约束

1. 保留原始 DeepCAD 的生成闭包：CAD 命令与参数的主解码路径必须建模为 `P(S | z)`，即解码器在训练和推理时均以潜变量 `z` 作为唯一必需条件。随机采样 `z` 应能够独立生成 CAD 序列，因此主解码器不得把 `constraint_memory` 作为生成命令或参数的必要输入。
2. 约束融合仅作为结构化表征与训练监督增强：`constraint_tags`、约束 token、unary / pair 关系矩阵等信息可以进入编码器、辅助预测头或评估流程，用于提升潜变量对水平、竖直、平行、垂直关系的表达能力，但不得形成推理阶段必须外部提供的约束依赖。
3. 所有约束相关损失必须作用于 Decoder Layer 之后的可解释输出或中间解码表示，例如命令 logits、参数 logits、线级约束预测、关系重建结果或由解码输出解析得到的软几何。不得直接基于 `z` 本身计算几何一致性或约束满足损失。
4. 多任务目标以原始 DeepCAD 的命令分类与参数离散值重建为主任务，约束预测、关系重建和几何一致性损失为辅助项。辅助损失应服务于最终 CAD 序列的几何关系保持，不能替代或绕过主解码路径。
