---
alwaysApply: false
---

# DeepCAD / 约束融合类方案 — 固定训练与评估流程（抽象）

本规则描述**与具体方案名无关**的固定套路，便于在多个实验包之间复用。某一方案的入口模块名、CLI 参数与默认路径以该方案目录下的 `train.py` / `evaluate*.py` / `config_*.py` 为准。

**记号**：`<repo>` 为仓库根目录；`<pkg>` 为可 `python -m` 运行的方案包（例如 `some_experiment_pkg`）；`<exp>` 为实验名（`exp_name`）。

## 1. 环境与数据前置

- 在 `<repo>` 下执行 `python -m <pkg>.…`，保证包导入与相对路径一致。
- 数据根目录一般为 `data/`，划分文件常为 `data/train_val_test_split.json`（若方案沿用主仓库数据集）。
- 依赖以该方案实际 import 为准；GPU 通常通过 `-g` / `CUDA_VISIBLE_DEVICES` 或方案自带的 `--device` 指定。冒烟可用小 `batch_size`、`num_workers=0`，必要时 CPU。

## 2. 实验目录约定（建议所有方案对齐）

在方案内统一采用可预测的落盘结构，便于对比与归档：

| 角色 | 典型路径（相对 `<repo>`） |
| --- | --- |
| 工程根 | `proj_log/<方案族或项目名>/` → 作为 `proj_dir` |
| 单次实验 | `{proj_dir}/<exp>/` → `exp_dir` |
| 权重 | `{exp_dir}/model/`（`latest`、按 epoch 命名等，以实现为准） |
| 训练日志 | `{exp_dir}/log/`（如 TensorBoard 事件文件） |
| 训练侧表格/清单 | `{exp_dir}/artifacts/`（如 `train_metrics.csv`、`manifest.json`） |
| 配置快照 | `{exp_dir}/config.txt` 或等价 JSON（启动训练时写入完整 CLI/超参） |

新建 `exp_dir` 时：实现上常见「目录已存在且含 checkpoint 则拒绝覆盖」；续训用 `--continue`（或等价开关），清空重跑用 `--force_overwrite`（或等价开关）。具体名称以该方案 `Config` 为准。

## 3. 训练流程（固定步骤）

1. **确认入口**：`<pkg>.train`（或文档指定的单文件脚本）。
2. **固定参数三元组**：`proj_dir`、`data_root`、`exp_name` —— 三者唯一确定一次实验的磁盘位置与日志归属。
3. **启动训练**：`python -m <pkg>.train …`（Windows 多行可用 `^` 续行，Unix 用 `\`）。
4. **正式训练规模（本仓库惯例）**：一般按 **`--batch_size 64`**、**`--nr_epochs 100`** 跑满程，便于与既有实验对齐；若显存不足或某方案默认更小，再下调 `batch_size` 并建议在 `config` 快照里注明。冒烟、断点排查仍用小 `batch_size` / 少 epoch / `max_steps`（若支持），不替代该正式设定。
5. **训练中**：按方案实现周期性验证、学习率调度、梯度裁剪；关注 loss 是否 NaN、是否写盘正常。
6. **每个阶段结束**：保存可用于评估的 checkpoint；同时保证 `config` 快照与选用权重路径可追溯。

**短程冒烟（任意方案通用）**：小 epoch / `max_steps`（若支持）、独立 `exp_name`，用于验证「数据 → 前向 → 反向 → 存 ckpt」闭环，不代替正式训练结论。

## 4. 评估流程

### 4.1 固定步骤

约束融合类工作流在工程上通常拆成**可独立复跑**的两段（名称因包而异，顺序一致即可）：

1. **加载权重**：显式 `--model_path` 或默认 `{exp_dir}/model/…`，与训练时结构开关一致。
2. **逐样本导出中间结果**（常见为重建的离散 CAD 向量）：写入 `reconstruction_dir` 下按样本命名的文件（如 `*_vec.h5`）；可通过 `eval_split`（`test` / `validation` 等）控制数据子集。
3. **在固定评估口径上聚合**：由评估脚本读取上一步目录中全部样本文件，写出 **`per_sample_counts.csv`**（逐样本明细）与 **`summary.json`**（全集汇总）。**论文级结论以该二者 + 同口径基线对比为准**，不单看训练 loss。
4. **仅重算指标**：若中间文件已齐全，使用方案提供的「跳过重建、只读目录聚合」开关（如 `skip_reconstruct` 类），避免重复推理。

**子集调试**：若支持 `sample_count` 或等价参数，用于快速打通评估脚本，不用于报告最终指标。

### 4.2 指标计算口径

评估分**两层**：**离散序列准确率**（DeepCAD 论文主指标）与**约束/几何恢复率**（轴对齐、平行/垂直 recall 等）。二者共用同一批 `*_vec.h5`，但由不同脚本聚合；实验记录 §4 中的列名与 `summary.json` / `*_acc_stat.txt` 字段对应关系见下表。

#### 公共前提

| 项 | 约定 |
| --- | --- |
| 中间文件 | `{reconstruction_dir}/{sample_id}_vec.h5`，含 `out_vec`（预测）、`gt_vec`（真值），均为整型离散 CAD 向量 `[T, 1+N_ARGS]` |
| 序列长度 | 重建时通常按 **GT 首个 `EOS` 位置**截断后再写入 h5（与 `test.py -m rec` 一致） |
| 解码口径 | 对 command / args logits 做 **argmax**（必要时对无效参数位填 `PAD_VAL`），即评估 **\(P(S\|z)\)** 硬解码结果，**不**在评测阶段注入外部约束 memory |
| 几何解析 | `CADSequence.from_vector(vec, is_numerical=True, n=grid_size)`；默认 **`grid_size=256`** |
| 角度阈值 | 约束类指标默认 **`angle_thresh=0.1`（度）**；无向夹角 `undirected_angle_deg(u,v) = min(θ, 180°−θ)` |
| 数据子集 | 与训练/config 快照中的 **`eval_split`** 一致（通常为 `test`） |

#### 离散序列指标：`ACC_cmd` / `ACC_param`

**脚本**：`evaluation/evaluate_ae_acc.py --src {reconstruction_dir}` → 写出 `{reconstruction_dir}_acc_stat.txt`。

**逐样本**

1. 令 `out_cmd = out_vec[:,0]`，`gt_cmd = gt_vec[:,0]`；参数列为 `[:,1:]`。
2. **`ACC_cmd`（样本级）** = 所有 token 位置上 `out_cmd == gt_cmd` 的比例（对序列长度求均值）。
3. **`ACC_param`（样本级）** 仅统计同时满足以下条件的 token：
   - `out_cmd == gt_cmd`（**命令必须先预测正确**才计入参数）；
   - `gt_cmd` 不为 `SOL` / `EOS`；
   - 参数位由 `CMD_ARGS_MASK[cmd]` 标记为有效。
4. 有效参数比较规则（`TOLERANCE = 3`）：
   - 一般参数：`|out − gt| < 3` 视为正确；
   - **`Ext` 命令**：最后 2 个有效参数须 **严格相等**；
   - **`Arc` 命令**：索引 3 的参数须 **严格相等**。
5. 样本级 `ACC_param` = 上述所有有效参数比较结果的均值。

**全集汇总**

- **`ACC_cmd`** = 全部测试样本的样本级 `ACC_cmd` 再求平均。
- **`ACC_param`** = 全部测试样本的样本级 `ACC_param` 再求平均。
- 脚本另输出各 command 类型、各参数槽的分项准确率（用于 Arc/Ext 等短板定位）。

#### 约束 / 几何指标：`ratio_h`、`ratio_v`、`parallel`、`perpendicular`

**脚本**（因方案而异，口径应对齐）：

- 约束融合 High Modify 等：`<pkg>.application.evaluate_constraints`（或 `<pkg>.evaluate`）→ `summary.json` + `per_sample_counts.csv`。
- 原始 DeepCAD 复现：`论文尝试/DeepCAD原始约束指标/eval_original_deepcad_axis_constraints.py`（`-m rec` 重建 + 聚合，或 `--skip_reconstruct` 只读目录）。

**解析失败计数**

- **`parse_fail`**（实验记录列名）= `summary.json` 的 **`n_parse_fail_pred`**：预测向量 `out_vec` 无法解析为 `CADSequence` 的样本数（GT 解析失败记入 `n_parse_fail_gt`，一般不单列）。

**Extrude 数量不一致**

- **`ext_mismatch`**（实验记录列名）= **`n_samples_extrude_count_mismatch`**：GT 与 pred 解析后 **Extrude 段数不同**的样本数。

**平行 / 垂直 recall（index-aligned，实验记录列 `parallel` / `perpendicular`）**

对每个样本、每个 extrude 索引 `k`（取 `min(n_gt_ext, n_pred_ext)`）：

1. 分别收集 GT / pred 该 extrude 内所有 `Line`，按 profile 顺序排列。
2. 若 **GT 与 pred 的 line 数量不等**，该 extrude **跳过**（不计入 pair 分母）。
3. 在 GT line 集合上用 `extract_raw_from_lines` 得到平行对 `(i,j)`、垂直对 `(i,j)`（**分母仅含 GT 定义的 pair**）。
4. 在 **pred 的同名索引** `(i,j)` 上检查：
   - **平行 hit**：`undirected_angle_deg(pred_dir_i, pred_dir_j) < angle_thresh`；
   - **垂直 hit**：`|undirected_angle_deg(...) − 90°| < angle_thresh`。
5. 全集 **`parallel`**（=`parallel_recall_index_aligned`）= `Σ par_hits / Σ par_denom`；
   **`perpendicular`**（=`perpendicular_recall_index_aligned`）= `Σ perp_hits / Σ perp_denom`。
6. 若全集分母为 0，对应指标为 `null`（JSON 中可能带 `*_defined: false`）。

**水平 / 竖直恢复率 `ratio_h` / `ratio_v`（注意两套口径）**

| 口径 | 适用脚本 | 定义 |
| --- | --- | --- |
| **Index-aligned 恢复（推荐，High Modify `evaluate_constraints`）** | `constraint_fused_*_high_modify` 等 | 对每个 extrude，在 **GT 与 pred line 数相等** 时：分母 = GT 判为 horizontal（或 vertical）的 **line 索引数**；分子 = 这些索引上 pred 方向仍与 +x（或 +y）夹角 `< angle_thresh` 的条数。全集 `ratio_h = Σ hits / Σ denom`（`ratio_v` 同理）。`summary.json` 中含 `ratio_h_definition` 文字说明。 |
| **全局计数比（早期 DeepCAD 约束脚本）** | `eval_original_deepcad_axis_constraints.py` | 逐样本 `pred_h / gt_h`（GT 上 horizontal Line **条数**之比），全集 `ratio_h = Σ pred_h / Σ gt_h`（`ratio_v` 同理）。**与 index-aligned 数值不可直接对比**。 |

对比实验时必须在记录中注明**使用的评估脚本与 `ratio_*` 口径**；本仓库主路径消融实验记录默认采用 **index-aligned**（High Modify 评估链）。

#### 实验记录 ↔ 产物字段对照

| 实验记录 §4 列 | 主要来源 | JSON / 文件键 |
| --- | --- | --- |
| `ACC_cmd` | `evaluate_ae_acc.py` | `avg command acc (ACC_cmd)` |
| `ACC_param` | 同上 | `avg param acc (ACC_param)` |
| `ratio_h` | 约束聚合 | `summary.json` → `ratio_h` |
| `ratio_v` | 同上 | `ratio_v` |
| `parallel` | 同上 | `parallel_recall_index_aligned` |
| `perpendicular` | 同上 | `perpendicular_recall_index_aligned` |
| `parse_fail` | 同上 | `n_parse_fail_pred` |
| `ext_mismatch` | 同上 | `n_samples_extrude_count_mismatch` |

#### 典型命令链（示例）

```bash
# 原始 DeepCAD 路径（D0 等）
python test.py --proj_dir proj_log --exp_name newDeepCAD_2 -m rec --ckpt 80 --eval_split test \
  -o proj_log/newDeepCAD_2/results/test_80
python evaluation/evaluate_ae_acc.py --src proj_log/newDeepCAD_2/results/test_80
python 论文尝试/DeepCAD原始约束指标/eval_original_deepcad_axis_constraints.py \
  --proj_dir proj_log --exp_name newDeepCAD_2 --ckpt 80 --skip_reconstruct \
  --reconstruction_dir proj_log/newDeepCAD_2/results/test_80 \
  --out_dir proj_log/newDeepCAD_2/artifacts/test_eval_epoch80

# 约束融合 High Modify 路径（H1 / A* 等）
python -m constraint_fused_deepcad_simplify_modify2_low_riskbased_high_modify.evaluate \
  --proj_dir proj_log/constraint_fused_deepcad_simplify_modify2_low_riskbased_high_modify \
  --exp_name cf_high_modify_cad_only_100 --ckpt 20 --eval_split test \
  --skip_reconstruct --reconstruction_dir .../results/test_20
# 同上包默认一步完成重建+聚合时，省略 --skip_reconstruct
```

**禁止混用**：不同 `eval_split`、`grid_size`、`angle_thresh`、或不同 `ratio_h` 口径的 `summary.json` 不得与论文主表直接对比；跨方案对比时须复跑**同一评估脚本**。

## 5. 跨方案复现时必须对齐的信息

切换方案或对比实验时，下列内容应在 `config` 快照与评估命令中**与训练时一致**（具体 flag 名以各方案为准）：

- 数据路径与 `eval_split`。
- 网格/量化与几何阈值（如 `grid_size`、`angle_thresh`、`dist_thresh` 等）。
- 影响前向图结构的开关（编码器/解码器分支、dropout、是否注入约束 memory 等）。
- 损失权重与任何「仅训练期启用」的正则，需明确评估期解码路径是否与论文声明的推理设定一致（见下节架构约定）。

## 6. 可选辅助入口（按方案是否提供）

- **随机或条件生成冒烟**：`infer` / `generate` 类模块，用于检查 `z` 解码或管线连通性，**不替代**第 4 节的统一指标评估。
- **基线快照 / 单测**：`baseline_snapshot`、`verify_*` 等，用于冻结对比数字或做前向/反向自检。

是否提供、模块路径以各方案 `README` 或包内 `application/` 为准；本规则不绑定具体文件名。

## 7. 归档与对比检查清单

- [ ] `exp_dir` 内可找到与报告对应的 **checkpoint 路径** 与 **config 快照**。
- [ ] 同一次评估的 **`summary.json` + `per_sample_counts.csv`** 与重建目录一并归档（或可从命令复现）。
- [ ] 记录：`eval_split`、评估脚本版本（commit）、关键结构开关状态。
- [ ] 与基线对比时使用**同一评估脚本、同一阈值定义**（见 §4.2），避免混用不同口径。

## 8. 与 CF-DeepCAD 架构约束的关系

主生成闭包与约束融合边界以 `.cursor/rules/CF-DeepCADAgreement.mdc` 为准：主解码路径建模为 `P(S|z)`；约束相关信号用于编码/辅助头/训练监督时的约定，以及「损失不得直接打在裸 `z` 上」等，在实现新方案时仍应遵守，与具体包名无关。
